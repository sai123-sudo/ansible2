- name: play level variables
  hosts: local
  connection: local
  vars:
   COURSE: ansible   #eql ro COURSE=Ansible in shell , shell we refewr $VAR 0r ${VAR}
   DURATION: 10HR
   TRAINER: Sivakumar
  tasks:
  - name: print course information
    ansible.builtin.debug:
    #vars referense should be surrounded by ""
     msg: "Hello {{ COURSE }}, Course Duration {{ DURATION }}, tRAINER IS {{ TRAINER }}"